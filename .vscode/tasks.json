{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build",
            "problemMatcher":"$msCompile",
            
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command":"cmake --build build"
        }
    ]
}